rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isAuthenticated() {
      return request.auth != null;
    }

    function isUser(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    match /usernames/{usernameLower} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && request.resource.data.uid == request.auth.uid;
      allow update: if isAuthenticated()
        && resource.data.uid == request.auth.uid
        && request.resource.data.uid == request.auth.uid;
      allow delete: if isAuthenticated() && resource.data.uid == request.auth.uid;
    }

    match /users/{userId} {
      allow read: if isAuthenticated();
      allow create: if isUser(userId) && request.resource.data.uid == userId;

      // Profile owners may edit profile fields, but never follower count.
      // followersCount may change only in the same atomic operation as the
      // matching follower/following relationship transition.
      allow update: if isAuthenticated() && (
        (
          isUser(userId)
          && request.resource.data.uid == userId
          && request.resource.data.diff(resource.data).affectedKeys()
              .hasNone(['followersCount'])
        )
        || (
          request.resource.data.diff(resource.data).affectedKeys()
              .hasOnly(['followersCount'])
          && (
            (
              (resource.data.followersCount == null
                && request.resource.data.followersCount == 1)
              || request.resource.data.followersCount == resource.data.followersCount + 1
            )
            && !exists(/databases/$(database)/documents/users/$(userId)/followers/$(request.auth.uid))
            && existsAfter(/databases/$(database)/documents/users/$(userId)/followers/$(request.auth.uid))
            && !exists(/databases/$(database)/documents/users/$(request.auth.uid)/following/$(userId))
            && existsAfter(/databases/$(database)/documents/users/$(request.auth.uid)/following/$(userId))
          )
          || (
            resource.data.followersCount > 0
            && request.resource.data.followersCount == resource.data.followersCount - 1
            && exists(/databases/$(database)/documents/users/$(userId)/followers/$(request.auth.uid))
            && !existsAfter(/databases/$(database)/documents/users/$(userId)/followers/$(request.auth.uid))
            && exists(/databases/$(database)/documents/users/$(request.auth.uid)/following/$(userId))
            && !existsAfter(/databases/$(database)/documents/users/$(request.auth.uid)/following/$(userId))
          )
        )
      );

      allow delete: if isUser(userId);

      match /blockedUsers/{blockedId} {
        allow read: if isAuthenticated() && (request.auth.uid == userId || request.auth.uid == blockedId);
        allow write: if isUser(userId);
      }

      match /followers/{followerId} {
        allow read: if isAuthenticated();

        allow create: if isUser(followerId)
          && request.resource.data.followerId == followerId
          && existsAfter(
            /databases/$(database)/documents/users/$(userId)/following/$(followerId)
          )
          && (
            (
              get(/databases/$(database)/documents/users/$(userId)).data.followersCount == null
              && getAfter(/databases/$(database)/documents/users/$(userId)).data.followersCount == 1
            )
            || getAfter(/databases/$(database)/documents/users/$(userId)).data.followersCount
                == get(/databases/$(database)/documents/users/$(userId)).data.followersCount + 1
          );

        allow delete: if isUser(followerId)
          && exists(
            /databases/$(database)/documents/users/$(userId)/following/$(followerId)
          )
          && !existsAfter(
            /databases/$(database)/documents/users/$(userId)/following/$(followerId)
          )
          && get(/databases/$(database)/documents/users/$(userId)).data.followersCount > 0
          && getAfter(/databases/$(database)/documents/users/$(userId)).data.followersCount
              == get(/databases/$(database)/documents/users/$(userId)).data.followersCount - 1;
      }

      match /following/{followingId} {
        allow read: if isAuthenticated();

        allow create: if isUser(userId)
          && request.resource.data.followingId == followingId
          && existsAfter(
            /databases/$(database)/documents/users/$(followingId)/followers/$(userId)
          );

        allow delete: if isUser(userId)
          && exists(
            /databases/$(database)/documents/users/$(followingId)/followers/$(userId)
          )
          && !existsAfter(
            /databases/$(database)/documents/users/$(followingId)/followers/$(userId)
          );
      }

      match /friends/{friendId} {
        allow read: if isAuthenticated();
        allow write: if isAuthenticated() && (isUser(userId) || request.auth.uid == friendId);
      }

      match /bookmarks/{bookmarkId} {
        allow read, write: if isUser(userId);
      }
    }

    match /reports/{reportId} {
      allow create: if isAuthenticated() && request.resource.data.reporterId == request.auth.uid;
      allow read, update, delete: if false;
    }

    match /posts/{postId} {
      allow read: if isAuthenticated();

      // New posts always start with zero interaction counters. Clients cannot
      // choose arbitrary non-zero values at creation time.
      allow create: if isAuthenticated()
        && request.resource.data.authorId == request.auth.uid
        && request.resource.data.likesCount == 0
        && request.resource.data.commentsCount == 0
        && request.resource.data.repostsCount == 0;

      // A normal post can be deleted by its author. A repost can be deleted
      // only together with its repost marker and original-post counter update.
      allow delete: if isAuthenticated()
        && resource.data.authorId == request.auth.uid
        && (
          resource.data.isRepost != true
          || (
            resource.data.originalPostId is string
            && exists(
              /databases/$(database)/documents/posts/$(resource.data.originalPostId)
                /reposts/$(request.auth.uid)
            )
            && !existsAfter(
              /databases/$(database)/documents/posts/$(resource.data.originalPostId)
                /reposts/$(request.auth.uid)
            )
            && get(
              /databases/$(database)/documents/posts/$(resource.data.originalPostId)
            ).data.repostsCount > 0
            && getAfter(
              /databases/$(database)/documents/posts/$(resource.data.originalPostId)
            ).data.repostsCount
              == get(
                /databases/$(database)/documents/posts/$(resource.data.originalPostId)
              ).data.repostsCount - 1
          )
        );

      // The author may edit normal post content but cannot modify any
      // interaction counter. Counter changes are allowed only when the
      // corresponding interaction document changes in the same atomic write.
      allow update: if isAuthenticated() && (
        (
          resource.data.authorId == request.auth.uid
          && request.resource.data.authorId == resource.data.authorId
          && request.resource.data.createdAt == resource.data.createdAt
          && request.resource.data.diff(resource.data).affectedKeys()
              .hasNone(['likesCount', 'commentsCount', 'repostsCount'])
        )
        || (
          request.resource.data.diff(resource.data).affectedKeys().hasOnly(['likesCount'])
          && (
            (
              resource.data.likesCount == null
              && request.resource.data.likesCount == 1
              && !exists(/databases/$(database)/documents/posts/$(postId)/likes/$(request.auth.uid))
              && existsAfter(/databases/$(database)/documents/posts/$(postId)/likes/$(request.auth.uid))
            )
            || (
              request.resource.data.likesCount == resource.data.likesCount + 1
              && !exists(/databases/$(database)/documents/posts/$(postId)/likes/$(request.auth.uid))
              && existsAfter(/databases/$(database)/documents/posts/$(postId)/likes/$(request.auth.uid))
            )
            || (
              resource.data.likesCount > 0
              && request.resource.data.likesCount == resource.data.likesCount - 1
              && exists(/databases/$(database)/documents/posts/$(postId)/likes/$(request.auth.uid))
              && !existsAfter(/databases/$(database)/documents/posts/$(postId)/likes/$(request.auth.uid))
            )
          )
        )
        || (
          request.resource.data.diff(resource.data).affectedKeys().hasOnly(['commentsCount'])
          && (
            (
              resource.data.commentsCount == null
              && request.resource.data.commentsCount == 1
              && getAfter(
                /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
              ).data.type == 'comment_create'
              && getAfter(
                /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
              ).data.targetId is string
              && !exists(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(getAfter(
                    /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
                  ).data.targetId)
              )
              && existsAfter(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(getAfter(
                    /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
                  ).data.targetId)
              )
            )
            || (
              request.resource.data.commentsCount == resource.data.commentsCount + 1
              && getAfter(
                /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
              ).data.type == 'comment_create'
              && getAfter(
                /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
              ).data.targetId is string
              && !exists(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(getAfter(
                    /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
                  ).data.targetId)
              )
              && existsAfter(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(getAfter(
                    /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
                  ).data.targetId)
              )
            )
            || (
              resource.data.commentsCount > 0
              && request.resource.data.commentsCount == resource.data.commentsCount - 1
              && getAfter(
                /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
              ).data.type == 'comment_delete'
              && getAfter(
                /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
              ).data.targetId is string
              && exists(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(getAfter(
                    /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
                  ).data.targetId)
              )
              && !existsAfter(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(getAfter(
                    /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
                  ).data.targetId)
              )
            )
          )
        )
        || (
          request.resource.data.diff(resource.data).affectedKeys().hasOnly(['repostsCount'])
          && (
            (
              (resource.data.repostsCount == null && request.resource.data.repostsCount == 1)
              || request.resource.data.repostsCount == resource.data.repostsCount + 1
            )
            && !exists(
              /databases/$(database)/documents/posts/$(postId)/reposts/$(request.auth.uid)
            )
            && existsAfter(
              /databases/$(database)/documents/posts/$(postId)/reposts/$(request.auth.uid)
            )
          )
          || (
            resource.data.repostsCount > 0
            && request.resource.data.repostsCount == resource.data.repostsCount - 1
            && exists(
              /databases/$(database)/documents/posts/$(postId)/reposts/$(request.auth.uid)
            )
            && !existsAfter(
              /databases/$(database)/documents/posts/$(postId)/reposts/$(request.auth.uid)
            )
          )
        )
      );

      match /likes/{userId} {
        allow read: if isAuthenticated();

        allow create: if isUser(userId)
          && request.resource.data.keys().hasOnly(['likedAt'])
          && getAfter(
            /databases/$(database)/documents/posts/$(postId)
          ).data.likesCount
            == (
              get(/databases/$(database)/documents/posts/$(postId)).data.likesCount + 1
            );

        allow delete: if isUser(userId)
          && get(/databases/$(database)/documents/posts/$(postId))
              .data.likesCount > 0
          && getAfter(
            /databases/$(database)/documents/posts/$(postId)
          ).data.likesCount
            == (
              get(/databases/$(database)/documents/posts/$(postId)).data.likesCount - 1
            );
      }

      match /counterOps/{userId} {
        allow read: if false;
        allow delete: if false;

        allow create, update: if isUser(userId)
          && request.resource.data.keys().hasOnly(['type', 'targetId'])
          && request.resource.data.targetId is string
          && (
            (
              request.resource.data.type == 'comment_create'
              && !exists(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(request.resource.data.targetId)
              )
              && existsAfter(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(request.resource.data.targetId)
              )
              && getAfter(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(request.resource.data.targetId)
              ).data.authorId == request.auth.uid
            )
            || (
              request.resource.data.type == 'comment_delete'
              && exists(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(request.resource.data.targetId)
              )
              && !existsAfter(
                /databases/$(database)/documents/posts/$(postId)/comments/
                  $(request.resource.data.targetId)
              )
              && (
                get(
                  /databases/$(database)/documents/posts/$(postId)/comments/
                    $(request.resource.data.targetId)
                ).data.authorId == request.auth.uid
                || get(
                  /databases/$(database)/documents/posts/$(postId)
                ).data.authorId == request.auth.uid
              )
            )
          );
      }

      match /comments/{commentId} {
        allow read: if isAuthenticated();

        allow create: if isAuthenticated()
          && request.resource.data.authorId == request.auth.uid
          && request.resource.data.likesCount == 0
          && existsAfter(
            /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
          )
          && getAfter(
            /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
          ).data.type == 'comment_create'
          && getAfter(
            /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
          ).data.targetId == commentId
          && getAfter(
            /databases/$(database)/documents/posts/$(postId)
          ).data.commentsCount
            == get(
              /databases/$(database)/documents/posts/$(postId)
            ).data.commentsCount + 1;

        allow delete: if isAuthenticated()
          && (
            resource.data.authorId == request.auth.uid
            || get(/databases/$(database)/documents/posts/$(postId)).data.authorId == request.auth.uid
          )
          && existsAfter(
            /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
          )
          && getAfter(
            /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
          ).data.type == 'comment_delete'
          && getAfter(
            /databases/$(database)/documents/posts/$(postId)/counterOps/$(request.auth.uid)
          ).data.targetId == commentId
          && resource.data.likesCount >= 0
          && getAfter(
            /databases/$(database)/documents/posts/$(postId)
          ).data.commentsCount
            == get(
              /databases/$(database)/documents/posts/$(postId)
            ).data.commentsCount - 1;

        allow update: if isAuthenticated() && (
          (
            resource.data.authorId == request.auth.uid
            && request.resource.data.authorId == resource.data.authorId
            && request.resource.data.diff(resource.data).affectedKeys()
                .hasOnly(['text', 'isEdited', 'editedAt'])
          )
          || (
            request.resource.data.diff(resource.data).affectedKeys().hasOnly(['likesCount'])
            && (
              (
                request.resource.data.likesCount == resource.data.likesCount + 1
                && !exists(
                  /databases/$(database)/documents/posts/$(postId)/comments/$(commentId)/likes/$(request.auth.uid)
                )
                && existsAfter(
                  /databases/$(database)/documents/posts/$(postId)/comments/$(commentId)/likes/$(request.auth.uid)
                )
              )
              || (
                resource.data.likesCount > 0
                && request.resource.data.likesCount == resource.data.likesCount - 1
                && exists(
                  /databases/$(database)/documents/posts/$(postId)/comments/$(commentId)/likes/$(request.auth.uid)
                )
                && !existsAfter(
                  /databases/$(database)/documents/posts/$(postId)/comments/$(commentId)/likes/$(request.auth.uid)
                )
              )
            )
          )
        );

        match /likes/{userId} {
          allow read: if isAuthenticated();

          allow create: if isUser(userId)
            && request.resource.data.keys().hasOnly(['likedAt'])
            && getAfter(
              /databases/$(database)/documents/posts/$(postId)/comments/$(commentId)
            ).data.likesCount
              == get(
                /databases/$(database)/documents/posts/$(postId)/comments/$(commentId)
              ).data.likesCount + 1;

          allow delete: if isUser(userId)
            && get(
              /databases/$(database)/documents/posts/$(postId)/comments/$(commentId)
            ).data.likesCount > 0
            && getAfter(
              /databases/$(database)/documents/posts/$(postId)/comments/$(commentId)
            ).data.likesCount
              == get(
                /databases/$(database)/documents/posts/$(postId)/comments/$(commentId)
              ).data.likesCount - 1;
        }
      }

      match /reposts/{userId} {
        allow read: if isAuthenticated();

        allow create: if isUser(userId)
          && request.resource.data.postId == postId
          && request.resource.data.reposterId == userId
          && (
            (
              get(/databases/$(database)/documents/posts/$(postId)).data.repostsCount == null
              && getAfter(/databases/$(database)/documents/posts/$(postId)).data.repostsCount == 1
            )
            || getAfter(/databases/$(database)/documents/posts/$(postId)).data.repostsCount
              == get(/databases/$(database)/documents/posts/$(postId)).data.repostsCount + 1
          );

        allow delete: if isUser(userId)
          && get(/databases/$(database)/documents/posts/$(postId)).data.repostsCount > 0
          && getAfter(/databases/$(database)/documents/posts/$(postId)).data.repostsCount
            == get(/databases/$(database)/documents/posts/$(postId)).data.repostsCount - 1;
      }
    }

    match /notifications/{notificationId} {
      allow read, update, delete: if isAuthenticated()
        && resource.data.recipientId == request.auth.uid;

      allow create: if isAuthenticated()
        && request.resource.data.actorId == request.auth.uid
        && request.resource.data.recipientId != request.auth.uid;
    }
  }
}
